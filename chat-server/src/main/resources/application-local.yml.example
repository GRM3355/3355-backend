# -----------------------------------------------------------------------------
# application-local.yml.example
# ? ??? 'application-local.yml'? ??? ?, <PLACEHOLDER>? ?? ???
# ???? ???. (.gitignore? ?? Git? ???? ??)
# -----------------------------------------------------------------------------
spring:
# =========================================================================
# 1. MongoDB Atlas ?? (?? ??? ???)
# =========================================================================
data:
mongodb:
uri: '{MongoDB Atlas? ?? ???}'

# =========================================================================
# 2. ElastiCache (Redis) ?? (??? Pub/Sub, ??? ???)
# =========================================================================
redis:
# ElastiCache Redis ????? ?? ?? (?? Redis ??)
host: '<ELASTICACHE_HOST_ENDPOINT>'
port: 6379
username: 'default'
password: '<ELASTICACHE_PASSWORD>'

# =========================================================================
# 3. RDS (PostgreSQL) ?? (? ?? ? ?? ?? ???)
# =========================================================================
datasource:
# RDS ????? ??
url: 'jdbc:postgresql://<RDS_HOST_ENDPOINT>:5432/zonie'
# username, password? ?? ?? ? ??, driver-class-name? ?? ? ?? ?? ? ??
# username: '<RDS_USERNAME>'
# password: '<RDS_PASSWORD>'
# driver-class-name: org.postgresql.Driver

# =========================================================================
# 4. JPA (?? ????? 'none'?? ???? ?, ??)
# =========================================================================
#jpa:
#hibernate:
#ddl-auto: update # ?? ???? ?? 'update' ?? 'create' ?? ??
#show-sql: true     # ????? SQL ?? ??

# =========================================================================
# 5. ?? ?? (??)
# =========================================================================
logging:
level:
root: INFO
com.grm3355.zonie: DEBUG # ????? ??? ?? ??